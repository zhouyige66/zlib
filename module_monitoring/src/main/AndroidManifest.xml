<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.roy.zlib.tool">

    <!--悬浮窗口权限-->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!--锁屏后台运行-->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <activity
            android:name="cn.roy.zlib.tool.component.ApplyAlertWindowPermissionActivity"
            android:launchMode="singleTask"
            android:theme="@style/StyleActivityTransparent" />
        <service android:name="cn.roy.zlib.tool.component.LogService" />

    </application>

</manifest>
